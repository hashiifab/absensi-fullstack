name: presensi_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  # sdk: ^3.6.0-134.0.dev
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  location: ^6.0.2
  syncfusion_flutter_maps: ^27.1.52
  http: ^1.2.2
  shared_preferences: ^2.3.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
